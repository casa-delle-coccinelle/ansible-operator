---
kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ ansible_operator_meta.name }}-options
  namespace: {{ ansible_operator_meta.namespace }}
data:
  options.list: |
{% for option in additional_ansible_options %}
    {{ option }}
{% endfor %}
